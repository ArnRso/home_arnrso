<form method="post" action="{{ path('bank_operation_category_delete', {'id': bank_operation_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bank_operation_category.id) }}">
    <button class="btn">{{ 'general.form.delete'|trans }}</button>
</form>
